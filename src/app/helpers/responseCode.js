


export const responseCode = (code) => {

    switch(code) {
        case 0:
            return "Email adress is not exsit"
        case 1:
            return "Email was sent"
        default:
    }
    
}